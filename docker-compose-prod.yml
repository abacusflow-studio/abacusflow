# 全面启用托管故而废弃

#include:
#  - path: ./docker-compose-base.yml
#
#services:
#  abacusflow-server:
#    container_name: abacusflow-server
#    image: ghcr.io/abacusflow/abacusflow-server:${ABACUSFLOW_SERVER_VERSION}
#    environment:
#      - POSTGRES_HOST=${POSTGRES_HOST}
#      - POSTGRES_DB=${POSTGRES_DB}
#      - POSTGRES_USER=${POSTGRES_USER}
#      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
#      - ABACUSFLOW_CUBE_API_SECRET=${ABACUSFLOW_CUBE_API_SECRET:?ABACUSFLOW_CUBE_API_SECRET must be set}
#    networks:
#      - abacusflow-network
#    depends_on:
#      - postgres
#    expose:
#      - 8080
#    ports:
#      - "${ABACUSFLOW_SERVER_PORT}:8080"
#  cube-db-provisioner:
#    image: postgres:${POSTGRES_VERSION}
#    environment:
#      - POSTGRES_HOST=postgres
#      - POSTGRES_PORT=${POSTGRES_PORT}
#      - POSTGRES_DB=${POSTGRES_DB}
#      - POSTGRES_USER=${POSTGRES_USER}
#      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
#      - POSTGRES_USER=${POSTGRES_USER:-abacusflow_api}
#      - POSTGRES_CUBE_USER=${POSTGRES_CUBE_USER:-abacusflow_cube}
#      - POSTGRES_CUBE_PASSWORD=${POSTGRES_CUBE_PASSWORD:?POSTGRES_CUBE_PASSWORD must be set}
#    volumes:
#      - ./script/provision-cube-role.sh:/opt/abacusflow/provision-cube-role.sh:ro
#    entrypoint: ["/bin/sh", "/opt/abacusflow/provision-cube-role.sh"]
#    networks:
#      - abacusflow-network
#    depends_on:
#      - postgres
#      - abacusflow-server
#    restart: "no"
#  cube:
#    environment:
#      - CUBEJS_DEV_MODE=false
#    depends_on:
#      cube-db-provisioner:
#        condition: service_completed_successfully
#  abacusflow-webapp:
#    container_name: abacusflow-webapp
#    image: ghcr.io/abacusflow/abacusflow-webapp:${ABACUSFLOW_WEBAPP_VERSION}
#    environment:
#      - REDIRECT_TO_HTTPS="false"
#      - ABACUSFLOW_SERVER_HOST=${ABACUSFLOW_SERVER_HOST}
#      - ABACUSFLOW_SERVER_PORT=${ABACUSFLOW_SERVER_PORT}
#      - ABACUSFLOW_CUBE_HOST=${ABACUSFLOW_CUBE_HOST}
#      - ABACUSFLOW_CUBE_PORT=${ABACUSFLOW_CUBE_PORT}
#    volumes:
#      - /etc/letsencrypt/output/abacusflow.cn/fullchain.pem:/etc/nginx/ssl/fullchain.pem:ro
#      - /etc/letsencrypt/output/abacusflow.cn/privkey.pem:/etc/nginx/ssl/privkey.pem:ro
#    networks:
#      - abacusflow-network
#    depends_on:
#      - abacusflow-server
#      - cube
#    ports:
#      - "${ABACUSFLOW_WEBAPP_HTTP_PORT}:80"
##      - "${ABACUSFLOW_WEBAPP_HTTPS_PORT}:443"
#
