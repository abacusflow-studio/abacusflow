# Mobile Backup

This directory keeps code that is no longer part of the active mobile information architecture.

- `legacy-management/`: old hidden management routes and order-entry/detail screens that are not reachable from the current primary tabs.
- `template/`: Expo starter template components, sample modal route, and unused React logo assets.

The mobile TypeScript and ESLint configs exclude this directory so backup code does not affect active builds.
